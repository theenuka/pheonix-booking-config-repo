apiVersion: networking.istio.io/v1beta1
kind: Gateway
metadata:
  name: main-gateway
  namespace: istio-ingress
spec:
  selector:
    istio: ingress
  servers:
  # Port 80: HTTP Traffic
  - port:
      number: 80
      name: http
      protocol: HTTP
    hosts:
    - "*"
    # tls:
    #   httpsRedirect: true

  # Port 443: Secure Traffic (Wildcard)
  - port:
      number: 443
      name: https
      protocol: HTTPS
    hosts:
    - "*.pheonix.live"
    tls:
      mode: SIMPLE
      credentialName: pheonix-live-tls  # Uses your new wildcard cert
