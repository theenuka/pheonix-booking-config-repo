apiVersion: v1
kind: Service
metadata:
  name: user-service
spec:
  ports:
  - port: {{ .Values.service.port }}
    targetPort: {{ .Values.service.port }}
    protocol: TCP
    name: http-app # Istio will use this name for protocol detection
  selector:
    app: user-service
  type: ClusterIP
